language: python
python: 2.6
env:
  - TOX_ENV=py26-1.6
  - TOX_ENV=py27-1.6
  - TOX_ENV=py27-1.7
  - TOX_ENV=coverage
install:
  - pip install tox
  - pip install -r requirements.txt
script:
  - tox -e $TOX_ENV
